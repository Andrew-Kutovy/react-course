const baseURL = 'https://jsonplaceholder.typicode.com';

const posts = '/posts';
const comments = '/comments';
const cars = 'http://owu.linkpc.net/carsAPI/v1/cars';

const urls = {
    posts,
    comments,
    cars
}

export {
    baseURL,
    urls
}